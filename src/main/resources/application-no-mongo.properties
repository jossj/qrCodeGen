# Activate with: mvn spring-boot:run -Dspring-boot.run.profiles=no-mongo
# MongoDB is excluded at the @SpringBootApplication level; this profile
# activates InMemoryQrCodeRepository (NoMongoConfig) instead.
